{
  "name": "seo-audit-runner",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Standalone SEO audit automation runner. Pure HTTP API client for the SEO analyzer application — no access to the app's database, no main-app imports. Runner-owned state uses the built-in node:sqlite module.",
  "bin": {
    "seo-audit-runner": "bin/seo-audit-runner.js"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node bin/seo-audit-runner.js",
    "test": "node --test"
  },
  "dependencies": {}
}
